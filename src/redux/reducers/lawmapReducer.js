import { ACTIONS } from "../constant";

const defaultState = {
  nodes: [],
  edges: [],
};
export function lawmapReducer(state = defaultState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
